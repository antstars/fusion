services:
  fusion:
    build:
      context: .
    env_file:
      - .env
    ports:
      - "12180:8080"
    restart: unless-stopped
    container_name: fusion_rss
    volumes:
      - ./data:/data
